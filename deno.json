{
  "tasks": {
    "test": "rm -rf cov_profile && deno test --coverage=cov_profile --allow-read && deno fmt && deno coverage cov_profile",
    "recache": "rm -f deno.lock && deno cache -r ./deps.ts"
  }
}
